POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=api_development

# Rails can use DATABASE_URL to connect to the container
# Example (development):
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/api_development

